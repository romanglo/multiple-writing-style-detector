sudo: false

language:
  - python

python:
  - 2.7
  - 3.6

install:
  - pip install -r requirements.txt

script:
  - pycodestyle mwsd
  - pytest --verbose --capture=no mwsd
  - pip install .

notifications:
  email: false
